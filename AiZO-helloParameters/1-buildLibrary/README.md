## Hello Parameters library

To compile this library just use Makefile, by calling ```make```.

It will create a Linux ```.so``` library (equivalent of ```.dll``` in Windows or ```.dylib``` in MacOS).
